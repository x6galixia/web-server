<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="apple-touch-icon" sizes="180x180" href="/images/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon/favicon-16x16.png">
    <link rel="manifest" href="/images/favicon/site.webmanifest">

    <link rel="stylesheet" href="../../css/local/login.css" />
    <title><%= title %></title>
  </head>
  <body>
    <div class="container">
      <!-- Preview Section -->
      <div class="preview">
        <h2>Welcome Back!</h2>
        <p>
          Log in to access your account and continue your journey with us. We're
          glad to have you back!
        </p>
      </div>

      <!-- Form Section -->
      <div class="form-container">
        <form action="/login" method="POST">
          <h1>Login</h1>

          <% if (typeof error !== 'undefined') { %>
            <div class="message error-message">
              <%= error %>
            </div>
          <% } %>

          <% if (typeof success !== 'undefined') { %>
            <div class="message success-message">
              <%= success %>
            </div>
          <% } %>
          
          <!-- Email Field -->
          <div class="input-group">
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" value="<%= typeof email !== 'undefined' ? email : '' %>" class="<%= errorField === 'email' ? 'error-input' : '' %>" require />
            <div class="error" id="emailError"></div>
          </div>

          <!-- Password Field -->
          <div class="input-group">
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" value="<%= typeof password !== 'undefined' ? password : '' %>" class="<%= errorField === 'password' ? 'error-input' : '' %>" require />
            <div class="error" id="passwordError"></div>
          </div>

          <!-- Submit Button -->
          <button type="submit">Login</button>

          <!-- Signup Link -->
          <p>Don't have an account? <a href="/signup">Sign up here</a>.</p>
        </form>
      </div>
    </div>
  </body>
</html>